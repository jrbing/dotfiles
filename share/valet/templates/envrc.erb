# Determine if program is in the PATH
# Usage: has ruby
#has()

# Add this to the PATH
# Usage: PATH_add bin
#PATH_add()

# Sets a common layout
# Usage: layout ruby
#   sets your gem home to .direnv/ruby-version
#   adds ./lib to your RUBYLIB
#   adds ./bin to your PATH
#layout()

# Imports another .envrc, if you want hierarchies
# Usage: source_env .more_env
#source_env()

# Ruby -------------------------------------------------------------------------
#RBENV_VERSION
#layout ruby
#PATH_add .direnv/bundler-bin
