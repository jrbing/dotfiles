# direnv environment file for golang project
# This file assumes that we'll be using the glide dependency
# management tool instead of simply using the go layout provided
# by direnv.

GOPATH=$(pwd)/_vendor
PATH=$(pwd)/_vendor/bin:$PATH
